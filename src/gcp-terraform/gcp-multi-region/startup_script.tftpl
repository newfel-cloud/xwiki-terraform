#! /bin/bash

sudo echo "${xwiki_01_private_ip} tf-init-deploy-basic-1" >> /etc/hosts
sudo echo "${xwiki_02_private_ip} tf-init-deploy-basic-2" >> /etc/hosts
sudo echo "${xwiki_03_private_ip} tf-init-deploy-basic-3" >> /etc/hosts
sudo echo "${xwiki_04_private_ip} tf-init-deploy-basic-4" >> /etc/hosts
bash /home/startup.sh ${db_ip} ${file_store_ip}